\(count : (Integer)) -> \(lastObservedTime : (../types/io.k8s.apimachinery.pkg.apis.meta.v1.MicroTime.dhall)) -> \(state : (Text)) -> 
{ count = count
, lastObservedTime = lastObservedTime
, state = state
}
